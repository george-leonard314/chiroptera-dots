# BlackArch packages, installed by `chiroptera-apps` only when the
# [blackarch] repository is configured (https://blackarch.org/downloads.html).
# Empty on purpose: add the security tools you want on a machine.
